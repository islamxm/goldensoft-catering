import { SelectComponentsConfig } from "react-select";
import { StateManagerProps } from "react-select/dist/declarations/src/useStateManager";

type PropsType = StateManagerProps & {
  label?: string  
}

export default PropsType;