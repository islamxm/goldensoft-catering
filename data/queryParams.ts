import QueryParamsType from "@/models/QueryParamsType";

const queryParams:Record<QueryParamsType, QueryParamsType> = {
  modalList: 'modalList',
  productId: 'productId',
}

export default queryParams;